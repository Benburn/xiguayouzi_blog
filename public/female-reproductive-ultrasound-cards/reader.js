document.addEventListener("DOMContentLoaded",()=>{const dialog=document.querySelector(".image-viewer");if(dialog){const target=dialog.querySelector("img");document.querySelectorAll(".source-copy img").forEach((img)=>img.addEventListener("click",()=>{target.src=img.src;target.alt=img.alt||"原书图片放大预览";dialog.showModal()}));dialog.querySelector("button").addEventListener("click",()=>dialog.close());dialog.addEventListener("click",(event)=>{if(event.target===dialog)dialog.close()})}const input=document.querySelector(".search input");if(input){input.addEventListener("input",()=>{const query=input.value.trim().toLowerCase();document.querySelectorAll(".topic-card").forEach((card)=>{card.hidden=query&&!card.textContent.toLowerCase().includes(query)});document.querySelectorAll(".index-group").forEach((group)=>{group.hidden=![...group.querySelectorAll(".topic-card")].some((card)=>!card.hidden)})})}});